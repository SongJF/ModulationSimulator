<UserControl x:Class="DrawChart.ChartCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrawChart"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="625" d:DesignWidth="1000">
    <Grid>
        <lvc:CartesianChart Series="{Binding _seriesCollection}" LegendLocation="Right" >
            <!--<lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Sales" LabelFormatter="{Binding _YFormatter}"></lvc:Axis>
            </lvc:CartesianChart.AxisY>-->
            <!--<lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Month" Labels="{Binding _Labels}"></lvc:Axis>
            </lvc:CartesianChart.AxisX>-->
        </lvc:CartesianChart>
    </Grid>
</UserControl>
